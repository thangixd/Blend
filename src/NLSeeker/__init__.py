"""Natural-language table discovery: registration, summarization, indexing and retrieval.

Submodules are imported lazily by their consumers so that the rest of Blend still works
without the optional `nl` dependency group installed.
"""
